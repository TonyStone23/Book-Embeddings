#---
# Helper functions
def expand():
    return

#---
# Embedder
class Embed:
    # Four Page Algorithm
    def fourPage(G):
        E = set(G.E())
        V = set(V.E())
        colored = set()
        positioned = set()

        while len(colored) < len(E):
            # position two vertices on either side of the layout
            # position a vertex between them.
            # .....
            pass
        return G